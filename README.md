# To-Do Reminder App

Це мобільний додаток-нагадувач, розроблений за допомогою React Native та Expo, який дозволяє користувачам додавати, переглядати, позначати як виконані та видаляти завдання з можливістю отримання push-сповіщень через OneSignal.

## 🏗️ Структура проекту

1.  [Вимоги](#вимоги)
2.  [Встановлення](#встановлення)
3.  [Конфігурація OneSignal](#конфігурація-onesignal)
4.  [Запуск проекту](#запуск-проекту)
5.  [Структура проекту](#структура-проекту)
6.  [Важливі імпорти та компоненти](#важливі-імпорти-та-компоненти)
    - [Компоненти](#компоненти)
    - [Хуки](#хуки)
    - [Сервіси](#сервіси)
    - [Стилі](#стилі)
    - [Основний застосунок](#основний-застосунок)
7.  [Скріншоти](#скріншоти)

## 🔧 Вимоги

Перш ніж почати, переконайтеся, що у вас встановлено:

- **Node.js** (рекомендується LTS версія)
- **npm** або **Yarn**
- **Expo CLI**: `npm install -g expo-cli` або `yarn global add expo-cli`
- Емулятор Android / iOS або фізичний пристрій з встановленим додатком Expo Go.

## Встановлення

1.  **Клонуйте репозиторій** (якщо ви використовуєте Git) або завантажте файли проекту.
2.  Перейдіть до кореневої папки проекту:
    ```bash
    cd your-project-folder
    ```
3.  Встановіть залежності проекту:
    ```bash
    npm install
    # або
    yarn install
    ```

## Конфігурація OneSignal

Цей проект використовує OneSignal для push-сповіщень. Вам потрібно буде створити обліковий запис OneSignal та налаштувати додаток:

1.  **Зареєструйтесь на [OneSignal](https://onesignal.com/)**
2.  **Налаштуйте платформу Android** Вам знадобиться `APP ID` та `REST API Key`.
3.  **Оновіть `app.json`**: Додайте `extra` секцію з вашими ключами OneSignal.
    ```json
    // app.json
    {
      "expo": {
        "extra": {
          "oneSignalAppId": "YOUR_ONESIGNAL_APP_ID", // Замініть на ваш App ID
          "oneSignalApiKey": "YOUR_ONESIGNAL_REST_API_KEY" // Замініть на ваш REST API Key
        }
      }
    }
    ```
4.  **Встановіть залежності OneSignal**:
    ```bash
    expo install react-native-onesignal
    ```

## Запуск проекту

Щоб запустити проект на вашому пристрої або емуляторі Android:

1.  Переконайтеся, що ваш емулятор Android запущений, або ваш фізичний пристрій підключений та дозволяє налагодження по USB.
2.  У кореневій папці проекту виконайте команду:
    ```bash
    npx expo run:android
    ```
    Ця команда збере пакет додатку та запустить його на підключеному пристрої або емуляторі.

## Структура проекту

Ось файл README.md з усім вмістом, який я щойно створив.

Markdown

# To-Do Reminder App

Це мобільний додаток-нагадувач, розроблений за допомогою React Native та Expo, який дозволяє користувачам додавати, переглядати, позначати як виконані та видаляти завдання з можливістю отримання push-сповіщень через OneSignal.

## Зміст

1.  [Вимоги](#вимоги)
2.  [Встановлення](#встановлення)
3.  [Конфігурація OneSignal](#конфігурація-onesignal)
4.  [Запуск проекту](#запуск-проекту)
5.  [Структура проекту](#структура-проекту)
6.  [Важливі імпорти та компоненти](#важливі-імпорти-та-компоненти)
    - [Компоненти](#компоненти)
    - [Хуки](#хуки)
    - [Сервіси](#сервіси)
    - [Стилі](#стилі)
    - [Основний застосунок](#основний-застосунок)
7.  [Скріншоти](#скріншоти)

## Вимоги

Перш ніж почати, переконайтеся, що у вас встановлено:

- **Node.js** (рекомендується LTS версія)
- **npm** або **Yarn**
- **Expo CLI**: `npm install -g expo-cli` або `yarn global add expo-cli`
- Емулятор Android / iOS або фізичний пристрій з встановленим додатком Expo Go.

## Встановлення

1.  **Клонуйте репозиторій** (якщо ви використовуєте Git) або завантажте файли проекту.
2.  Перейдіть до кореневої папки проекту:
    ```bash
    cd your-project-folder
    ```
3.  Встановіть залежності проекту:
    ```bash
    npm install
    # або
    yarn install
    ```

## Конфігурація OneSignal

Цей проект використовує OneSignal для push-сповіщень. Вам потрібно буде створити обліковий запис OneSignal та налаштувати додаток:

1.  **Зареєструйтесь на [OneSignal](https://onesignal.com/)** та створіть новий додаток.
2.  **Налаштуйте платформу Android** (та/або iOS) згідно з інструкціями OneSignal. Вам знадобиться `APP ID` та `REST API Key`.
3.  **Оновіть `app.json`**: Додайте `extra` секцію з вашими ключами OneSignal. Якщо її немає, додайте.
    ```json
    // app.json
    {
      "expo": {
        // ... інші налаштування
        "extra": {
          "oneSignalAppId": "YOUR_ONESIGNAL_APP_ID", // Замініть на ваш App ID
          "oneSignalApiKey": "YOUR_ONESIGNAL_REST_API_KEY" // Замініть на ваш REST API Key
        }
      }
    }
    ```
4.  **Встановіть залежності OneSignal**:
    ```bash
    expo install react-native-onesignal
    ```
    _Примітка: для Expo SDK 49+ `react-native-onesignal` може вимагати встановлення через `expo install` для коректної роботи з універсальним модулем._

## Запуск проекту

Щоб запустити проект на вашому пристрої або емуляторі Android:

1.  Переконайтеся, що ваш емулятор Android запущений, або ваш фізичний пристрій підключений та дозволяє налагодження по USB.
2.  У кореневій папці проекту виконайте команду:

    ```bash
    npx expo run:android
    ```

    Ця команда збере пакет додатку та запустить його на підключеному пристрої або емуляторі.

    Якщо ви хочете запустити на iOS або у веб-браузері:

    - Для iOS: `npx expo run:ios` (потрібен macOS та Xcode)
    - Для веб-браузера: `npm start` або `yarn start`, потім оберіть 'w' в терміналі.

## Структура проекту

lab-4/
├── .expo/ # Конфігураційні файли Expo
├── assets/ # Статичні ресурси (зображення, іконки)
│ └── foto.png  
├── components/ # UI компоненти
│ ├── AddTask.js # Форма для додавання нового завдання
│ ├── TaskCard.js # Одинична картка завдання
│ └── TasksList.js # Список завдань (нагадувань)
├── hooks/ # Користувацькі React хуки для логіки
│ └── useRemindersStorage.js # Хук для збереження/завантаження завдань у AsyncStorage
├── services/ # Сервіси та API взаємодії
│ └── oneSignalService.js # Функції для взаємодії з OneSignal API
├── screens/  
│ └── ToDo.js # Основний компонент логіки додатку
├── styles/ # Окремі файли стилів для компонентів
│ ├── AddTaskFormStyles.js
│ ├── ReminderCardStyles.js
│ ├── RemindersListStyles.js
│ └── ToDoStyles.js
├── app.json # Конфігурація проекту Expo
├── App.js # Головний компонент застосунку
├── package.json # Список залежностей та скриптів проекту
├── README.md # Цей файл
└──

## 📱 Скріншоти

---

![фото1](screenshots/1.png)

---

![фото2](screenshots/2.png)

- **OneSignal**

---

![фото3](screenshots/3.png)

## 👨‍💻 Автор

**Качур Віталій Васильович**, ВТк-24-1
