import React from "react";
import ActionItem from "./ActionItem";

const SettingsList = () => {
  return (
    <>
      <ActionItem title="Settings" onPress={() => {}} />
      <ActionItem title="Logout" onPress={() => {}} />
    </>
  );
};

export default SettingsList;
