import React from "react";
import ActionItem from "./ActionItem";

const SettingsList = () => {
  return (
    <>
      <ActionItem title="Remove Authenticator" onPress={() => {}} />
      <ActionItem title="My Recovery Code" onPress={() => {}} />
      <ActionItem title="Help" onPress={() => {}} />
    </>
  );
};

export default SettingsList;
